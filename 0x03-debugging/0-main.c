#include "main.h"

/**
* main - tests function that prints if integer is positive or negative
* Return: 0
*/

int main(void)
{
        int i;

        i = 0;  /* This line was changed to test the case of 0 */
        positive_or_negative(i);

        return (0);
}
