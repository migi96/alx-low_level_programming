#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument
 * @argv: array pointing to arguments
 *
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int m;

	for (i = 0; m < argc; m++)
	{
		printf("%s\n", argv[m]);
	}

	return (0);
}
